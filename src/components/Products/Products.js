import React from 'react';
import { categorieUnique } from '../../data.js'
 // Create category buttons
export default function Products(props) {
    const { setCategory } = props
    const categories = 
}